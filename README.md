# Prueba Técnica ISSSTESON — Sistema de Administración de Citas

Monorepo con:

- **backend/** — API REST en .NET 8 (arquitectura por capas, EF Core, SQL Server, JWT).
- **frontend/** — SPA en React 19 + TypeScript (Vite, TanStack Query/Router, Zustand, shadcn/ui).
